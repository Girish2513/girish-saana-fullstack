Git & GitHub Setup Notes

1. Git Configuration
- Installed Git inside WSL Linux environment
- Configured global username and email
- Verified configuration using git config --list

2. SSH Authentication
- Generated SSH key using ssh-keygen
- Added public key to GitHub account
- Tested authentication using ssh -T git@github.com
- Confirmed secure connection without password

3. Repository Setup
- Created repository named fullstack on GitHub
- Initialized local repository with git init
- Connected remote using SSH
- Verified connection using git remote -v

4. Branching Practice
- Created feature branches
- Committed changes separately
- Merged branches into main
- Resolved merge conflicts manually

5. Key Learning
Git is not just for saving code — it is a system for tracking changes, collaborating safely, and maintaining project history.
